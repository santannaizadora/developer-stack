// TODO

export const CreateAnswerData = (answer: string, question_id: number) => {
    return {
        answer,
        question_id
    }
}

const answerService = {};
export default answerService;